/// A collapsible, themeable side navigation drawer for Flutter.
///
/// The [DrawerRail] widget has two states: an expanded panel (with optional
/// search, section labels, a selected pill, badges and inline-expandable
/// groups) and a narrow icon rail where groups open as flyout menus. It has no
/// dependency on any state-management package — state lives in a
/// [DrawerRailController] you own.
///
/// See the `README.md` and the `example/` app for full usage.
library;

export 'src/animated_press_card.dart';
export 'src/drawer_model.dart';
export 'src/drawer_rail.dart';
export 'src/drawer_rail_controller.dart';
export 'src/drawer_rail_labels.dart';
export 'src/drawer_rail_theme.dart';
